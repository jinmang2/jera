---
name: jera-rag-architecture
description: Jera is a greenfield hexagonal offline-first RAG system at /home/jinmang2/jera
metadata: 
  node_type: memory
  type: project
  originSessionId: 81378544-4aea-4f92-8f99-8348882543e3
---

Jera (`/home/jinmang2/jera`) is a greenfield **hexagonal, offline-first RAG** system built 2026-06-12 via `/ralplan` consensus (Architect→Critic→Critic APPROVE). Plan/ADR: `.omc/plans/rag-redesign-plan.md`.

**Structure:** uv workspace. Domain in `python/jera/src/jera` (src-layout, package `jera`); FastAPI adapter-only in `apps/api/app` (package `jera-api`). Public facade: `import jera.rag`. Ports/adapters for parser, chunker, embedding, sparse, vector_store, metadata_store, reranker, generator.

**Profiles** (`JERA_PROFILE`, default `test`): `test` = fully deterministic offline (hash embedding, local BM25, in-memory vector store w/ RRF+DBSF fusion, SQLite, extractive generator — no Docker/torch/keys). `local` = fastembed ONNX (extra `local`). `prod` = Qdrant + Postgres + cloud, cloud disabled unless `JERA_ENABLE_CLOUD=1` + key.

**Env constraints:** Python 3.11 + uv present; Docker, psql, pnpm, ripgrep NOT available (so gates use pure-Python scans, not `rg`; storage abstracted behind ports).

**Gates:** `bash scripts/gates.sh` = ruff + ruff format + mypy strict (`mypy -p jera -p app`) + pytest. Fusion determinism frozen in `adapters/vector_store/fusion.py` (RRF k=60, 1-based ranks, missing=0, DBSF min-max, tie-break chunk_id asc) with golden test. Non-tautological hybrid-lift fixture frozen in `tests/unit/test_retrieval.py` (query "bravo india alpha").

**Milestones shipped (all on `main`):** M1 vertical slice (73ad12b) · M2 eval harness (800a8d0) · M3 semantic/hierarchical chunking + real Docling parser (209f8c3) · M4 Korean research RAG eval track + fastembed multilingual local + tool-use numeric QA (1793bf6, 155 tests). M4 built via `/deep-interview` → consensus plan → `/team` (3 workers). New in M4: `jera/tooluse/` (turn-based tool-use runtime + AST-safe CalculatorTool + FakeToolUseLLM + ToolAugmentedGenerator), `evaluation/{computation,matrix,gold_builder}.py`, EvalCase computation/table kinds. Plan `.omc/plans/rag-korean-research-toolqa-plan.md`, spec `.omc/specs/deep-interview-rag-korean-research-toolqa.md`, `QA_REPORT.md`.

**M4 corpus target:** Korean public-institution research reports (한국은행/KDI/자본시장연구원), freely-distributable only (copyright avoidance — sell-side analyst PDFs excluded; corpus PDFs git-ignored). Real ~20–30-case dataset is the user's opt-in build (`scripts/build_eval_dataset.py` + key).

**M5a shipped (b156623):** parser/OCR routing framework, LOCAL-first. Decisions: `DocumentParser` stays the only top-level port; `PageRouter`/`OCREngine` are adapter-internal Protocols (`adapters/parsing/routing.py`) composed inside `RoutingPdfParser` (per-page text|OCR routing, writes route/ocr provenance to element metadata, wired behind `use_routing_pdf` default off). `HwpxParser` (Hancom HWPX/OWPML) uses **stdlib zipfile+ElementTree, NOT lxml** (lxml is only in the docling extra, not base) — CI-real with a self-authored fixture. `parsing_metrics` (cer/table_f1/element_type_accuracy/reading_order Kendall-tau) + `parser_bench` graded vs a SEPARATE gold.json. Plan `.omc/plans/m5-parser-ocr-zoo-plan.md`. 171 tests.

User refs for M5: "한컴 그거" = **opendataloader-pdf** (Apache-2.0, Java11+ w/ pip binding; layout+table+OCR+VLM routing); HWP legacy via pyhwp; **rhwp** (Rust/WASM) is reference-only (SVG output, not a Python adapter). CLOVA OCR = disabled-by-default comparison only ("not yet" on paid). Env: Java 1.8 (opendataloader needs 11+), no tesseract → all heavy engines opt-in.

**M6 shipped (autopilot, 2026-06-13):** **Contextual Retrieval** (Anthropic 2024) + **RAGAS-lite gen-eval**, both offline/CI-real, default-off. Self-researched feature choice (user delegated). Decisions: new `Contextualizer` port (`ports/contextualizer.py`, batch `contextualize(document, chunks)->list[str]`); `Chunk` gained `context: str|None` + `embedding_text` property (`context + "\n\n" + text`) — `Chunk.text` NEVER mutated (citations/char_span intact), only indexing uses `embedding_text` (Contextual Embeddings + **Contextual BM25** — sparse `fit` + dense/sparse index both switched to `embedding_text`). `context` persisted via new nullable `ChunkRow.context` column (shared sql_store → SQLite+Postgres). Adapters: `HeuristicContextualizer` (title+section_path, deterministic, CI default-on when enabled) and `LlmContextualizer`(`SituateLLM` protocol; offline fake-tested) + `ClaudeSituateLLM` (opt-in, prompt-cached document block; `pragma: no cover`). Wired behind `use_contextual_retrieval` (default off) + `contextualizer_kind` (heuristic|llm; llm fails loud without cloud key). Non-tautological lift test (`test_contextual_retrieval.py`): answer chunk that never names "Acme" is WORST of 3 rival "outlook" chunks without context, BEST with it (idf-driven Contextual BM25). RAGAS-lite pure fns in `evaluation_contracts/generation_metrics.py`: `faithfulness`(containment≥0.6 sentence grounding), `answer_relevance`(cosine over supplied vectors), `answer_correctness`(multiset token-F1), `context_precision`(average-precision). Plan `.omc/plans/m6-contextual-retrieval-plan.md`. **198 tests** (was 171), all gates green offline. Pushed to origin/main (10009ff).

**M6b shipped (push+continue, 2026-06-13):** wired RAGAS-lite into the eval harness so the metrics actually run E2E. `QueryPipeline.answer_with_contexts()->AnsweredQuery{answer,contexts,retrieved_ids}` (frozen dataclass; `answer()` now delegates) + read-only `QueryPipeline.embedding` property. `EvalCase.reference_answer: str|None` (opt-in → enables answer_correctness + answer_relevance per case). New `GenerationEvalRunner` (`evaluation/generation_runner.py`) + `GenerationReport`/`GenerationCaseResult` (`evaluation/report.py`, optional means skip n/a cases). Exported via `jera.evaluation` + `jera.rag` facade. `scripts/eval.py` now prints a retrieval table AND a generation (RAGAS-lite) table. Tests `test_generation_runner.py` (faithfulness lands in (0,1) — extractive boilerplate preamble is correctly ungrounded; context_precision=1.0 for exact-match gold). **203 tests**, all gates green offline. Pushed to origin/main (6703aaa).

**M7 shipped (/ultrawork "keepgoing", 2026-06-13):** retrieval & answer quality levers, built as a parallel ultrawork wave — 2 background executor agents (Sonnet) for file-disjoint tracks (MMR reranker; generation-metrics-in-matrix) + inline integration/verification by the lead. (1) **MMRReranker** (`adapters/ranking/mmr_reranker.py`): greedy Maximal Marginal Relevance over the embedding — `lambda_*cos(q,d) - (1-lambda_)*max cos(d,selected)`; selectable via `reranker_kind="mmr"` + `mmr_lambda` (works any profile; `_build_reranker(settings, embedding)` now takes embedding). (2) **Multi-query retrieval**: new `QueryTransformer` port (`transform(query)->list[str]`, original-first); `RuleBasedExpander` (deterministic clause split on en/ko conjunctions, CI default) + `HydeTransformer` (HyDE, injected `HypothesisLLM`, fake-tested) + `ClaudeHypothesisLLM` (opt-in, pragma no-cover). `QueryPipeline.retrieve_multi()` retrieves each variant and RRF-fuses via the EXISTING `reciprocal_rank_fusion` (reused, frozen rules); `answer_with_contexts()` now routes through `retrieve_multi` (falls back to single `retrieve` when no transformer / single variant — `retrieve()` itself unchanged, so the retrieval gate is untouched). Wired behind `use_query_transform`/`query_transform_kind` (default off; hyde fails loud without cloud key). **Honest-fixture note:** a dramatic multi-query BM25 "lift" is NOT cleanly reproducible offline (idf already surfaces focused clause-answers; naive multi-query RRF can even amplify a majority topic) — so the non-tautological test instead proves the TRUE property: a real answer with ZERO lexical overlap with the literal query (score 0.0, ranked below an FAQ decoy) becomes the top result via a HyDE hypothesis that bridges the vocabulary gap (`test_multi_query_retrieval.py`). (3) Matrix: `StrategyEntry` gained mean_faithfulness/context_precision/answer_correctness/answer_relevance (backward-compat defaults), `run_matrix` runs `GenerationEvalRunner` per strategy, summary table extended. **235 tests** (was 203), all gates green offline (mypy 102 files). Pushed origin/main (d30fbe4).

**M8 shipped (/ultrawork hardening, 2026-06-13):** user pushback — "no 임시방편 (stopgaps); build the real paths properly, assume API/models available." Audit verdict: the offline test-doubles (FakeToolUseLLM/fakes-in-TESTS, hash embed, BM25, in-memory, extractive) are DELIBERATE documented design (keep — they're why CI is free/offline); the REAL gaps were (a) every opt-in cloud/vendor adapter was `# pragma: no cover` = written-but-unverified, (b) `ClaudeToolUseGenerator` not wired into registry (tooluse always served the fake even with cloud+key), (c) `pause_turn` unhandled, (d) `cost_metadata` placeholder, (e) korean_research.json a 3-case scaffold w/ placeholder chunk ids. NOTE on HyDE (the trigger): its real `ClaudeHypothesisLLM` WAS built+wired; `_FakeHyDE` is test-only — so HyDE followed the opt-in pattern, only its real adapter was pragma-no-cover. Fixes (parallel ultrawork: 3 background Sonnet executors + inline lead): **SDK-boundary verification** — fakes injected via `monkeypatch.setitem(sys.modules, "anthropic"/"openai"/"cohere"/"qdrant_client"/"fastembed"/"docling", fake)` so adapters' real `self._client=...(...)` + request-build + response-parse execute and are asserted, **method-level pragmas removed** (kept only on `except ImportError` branches). `test_cloud_anthropic_adapters.py` (51: ClaudeGenerator/SituateLLM/HypothesisLLM/ToolUseGenerator/GoldGenerator + pause_turn loop) + `test_cloud_vendor_adapters.py` (57: OpenAI/Cohere/Qdrant/fastembed×3/Docling/Postgres-via-shared-SqlStore). **pause_turn** now loops inside `ClaudeToolUseGenerator.call` (append resp.content to a LOCAL messages copy, re-call ≤8×, RuntimeError if exceeded). **`ClaudeToolUseGenerator` wired** into `_build_generator` (cloud+key→real, else FakeToolUseLLM). Real **`config/pricing.py`** (dated list prices: Anthropic/OpenAI/Cohere; local/fastembed=free via prefix match) populates `ProviderConfigSnapshot.cost_metadata` (placeholder gone). **Real Korean dataset**: 3 original synthetic Korean `.md` reports under `data/corpus/korean_research/` + `scripts/build_korean_dataset.py` (offline, deterministic stable_id chunk ids) → `data/eval/korean_research.json` 5 real cases (retrieval/table/computation, no placeholders); `test_korean_dataset.py` verifies gold resolves + recall@10==1.0. **355 tests** (was 235), all gates green offline. Remaining real forward-marker (NOT a stopgap): `routing_pdf_parser` VLM route raises NotImplementedError — that's the M5b VLM milestone (HeuristicRouter never emits VLM). Pushed origin/main (8715ed9).

**M5b shipped (/ultrawork parser/OCR zoo, 2026-06-13):** the opt-in parser/OCR zoo, built with the M8 discipline (real adapters + SDK-boundary verification + `requires_extra`; live-unrunnable in this env — no Java/tesseract/onnx — but genuinely built & tested). Parallel wave: 4 background Sonnet executors (each researched the REAL lib API via web before coding) + inline lead. New DocumentParser adapters (`adapters/parsing/`): `PyHwpParser` (legacy .hwp via `hwp5.filestructure.Hwp5File.preview_text.text` PrvText OLE stream — no lxml), `CamelotTableParser` (`camelot.read_pdf`→TABLE elements, table-only), `OpenDataLoaderParser` (`opendataloader_pdf.convert(format=json)`→typed elements, rich parser like docling, Java 11+). New OCREngine impls (`adapters/parsing/ocr/`, implement routing.py's `OCREngine` protocol): `TesseractOCREngine` (pytesseract image_to_string+image_to_data conf), `RapidOcrOCREngine` (rapidocr_onnxruntime, lazy-loads model on first recognize), `ClovaOCREngine` (NAVER CLOVA REST, disabled-by-default like cloud adapters). Boundary tests inject fake vendor modules via sys.modules and assert request-build + response-parse (`test_pyhwp_parser` 31, `test_camelot_parser` 26, `test_opendataloader_parser` 32, `test_ocr_engines` 44). **VLM route now FUNCTIONAL** (inline): `RoutingPdfParser(vlm=<OCREngine>)` — Route.VLM delegates to the injected engine + writes route="vlm" provenance (replaced the NotImplementedError stopgap; default HeuristicRouter still never emits VLM so default parsing unchanged); `test_routing_vlm.py`. Real **`scripts/parser_bench.py`** grades HwpxParser vs committed independent gold (1.0/1.0/1.0) + honestly reports each opt-in engine available/skipped. Inline wiring (lead owns all of these — agents touched only their adapter/test files): `parsing/__init__.py` exports, settings flags (`use_opendataloader`/`use_camelot`/`ocr_engine`/`ocr_lang`/`clova_*`), `_build_parsers` precedence (routing→opendataloader→docling→camelot→pymupdf; PyHwpParser always for .hwp) + `_build_ocr_engine` (fake→None, tesseract/rapidocr/clova; clova fails loud w/o key). KEY FIX during integration: OpenDataLoaderParser + RapidOcrOCREngine imported eagerly in __init__ → made LAZY (in parse()/first-recognize) so build_system constructs without the extras (consistent w/ docling pattern); odl ImportError tests moved __init__→parse(). New extras DEFINED in python/jera/pyproject.toml: `hwp`/`tables`/`opendataloader`/`ocr` (so `uv sync --extra X` is real) + mypy overrides for all vendor modules. **488 tests** (was 355), 8 skipped (requires_extra), all gates green offline (mypy 110 files). Pushed origin/main (c43cd51).

**M9 shipped (/ultrawork lifecycle+observability, 2026-06-13):** closed the real functional gap — the system couldn't delete/update documents, re-ingest duplicated, no operational visibility. Parallel wave: 3 background Sonnet executors (file-disjoint) + inline lead. (A) `VectorStore.delete(collection, chunk_ids)` on port + InMemory (pop) + Qdrant (`client.delete(points_selector=PointIdsList)`, boundary-tested), idempotent. (B) MetadataStore lifecycle: new frozen `DocumentInfo` (document_id/source_id/title/chunk_count) + 5 methods on port + SqlMetadataStore — `list_documents` (LEFT JOIN COUNT), `get_document_info`, `delete_document`→returns deleted chunk_ids (cascade), `chunk_ids_for_document`, `document_id_for_source`. (C) Query observability: `QueryStats` (timings_ms retrieve/rerank/generate/total + estimated_cost_usd via `estimate_query_cost()` chars/4 token heuristic × pricing.py + model_ids) on `AnsweredQuery.stats` (default None); `answer()` unchanged. INLINE (lead): **idempotent re-ingest** in IngestPipeline (delete_document→vector.delete before re-index; document_id is source-deterministic so content edits don't orphan chunks); `collection` added to RagSystem (for delete cleanup); new API routers — `GET /jobs/{id}` (poll), `GET /documents` + `GET/DELETE /documents/{id}` (cascade to vectors), `/query` now returns `stats`; schemas (JobResponse/DocumentInfoOut/DeleteResponse/QueryStatsOut). E2E tests (`test_api_e2e.py`): job poll, query stats present, full document list/get/delete lifecycle (deleted doc's vectors gone → query no longer cites it), re-ingest idempotency (2× ingest → 1 document). **536 tests** (was 488), all gates green offline. The system is now CRUD-complete + observable. Pushed origin/main (81707cb).

**M10 shipped (/ultrawork RESEARCH-driven 2025-26 SOTA, 2026-06-13):** user asked to self-research 25-26 technical SOTA, plan, implement before moving on. RESEARCH phase: 3 parallel document-specialist agents (retrieval / agentic / structure+eval) web-researched current SOTA → I synthesized. NOTE: document-specialist agents return a generic completion result; their synthesis had to be pulled via SendMessage / extracting the last assistant text block from the JSONL transcript (json-parse, filter text blocks). Picked 4 CI-realizable, non-tautologically-testable techniques; deferred (research-flagged as heavy/fine-tuning-bound) HippoRAG/GraphRAG-PPR, MRL+int8 quantization, RankLLM listwise, late-chunking, and SKIPPED Self-RAG/FLARE/CoRAG/DeepRAG (need fine-tuning or logprobs — incompatible with swappable frozen-model adapters). IMPLEMENT phase: 4 parallel Sonnet executors (file-disjoint) + inline facade wiring. (A) **Late-interaction ColBERT MaxSim** (ColBERTv2 NAACL'22; PLAID; Col-Bandit arXiv:2602.02827): new `MultiVectorEmbedding`+`MultiVectorStore` ports, `HashMultiVectorEmbedding` (per-token L2 vec seeded by sha1(token), position-independent), `MaxSimVectorStore` (pure-python Σ_q max_d cosine). Non-taut: MaxSim's per-token max beats single-vector bag when distractor's many tokens inflate L2 norm. (B) **Corrective RAG / CRAG** (Yan et al. ICLR'25 arXiv:2401.15884): `RetrievalEvaluator` port + `RetrievalGrade` enum + `OverlapRetrievalEvaluator` (score-gate + Jaccard, no LLM) + `CorrectiveQueryPipeline` (grade→if weak, transform+re-retrieve+RRF; relies on QueryTransformer's original-first contract so no double-count). Non-taut: scientific-vocab chunk goes 0→recall@1 via corrective re-query. (C) **Adaptive-RAG router** (Jeong NAACL'24; RAGRouter-Bench arXiv:2604.03455): `QueryRouter` port + `QueryComplexity` enum + `HeuristicQueryRouter` + `AdaptiveQueryPipeline` (NO_RETRIEVAL skips vector search entirely). Non-taut: counting-proxy proves 0 searches for NO_RETRIEVAL. (D) **Sub-question decomposition** (Pereira ACL-SRW'25 arXiv:2507.00355): `QueryDecomposer` port + `ConnectiveDecomposer` (5 en rules) + `DecompositionalQueryPipeline` (sequential retrieve per sub-q → accumulate unique chunks → 1 generate). Non-taut: multi-hop Eiffel→Paris→France, single-step covers 1 chunk, decomposed covers both. All offline LLM-judge/real-model variants are opt-in. Exported via `jera.rag` facade (42 symbols). **594 tests** (was 536, +58), all gates green offline (mypy 126 files). Citations in this session's plan. Pushed origin/main (5aae15b).

**M11 shipped (/ultrawork "ㄱ" = go, 2026-06-13):** the 4 research-viable-but-deferred techniques from M10's research (no new research wave — citations already in hand). 4 parallel Sonnet executors (file-disjoint) + inline registry/settings/facade/ingest wiring. (1) **HippoRAG PPR graph retrieval** (Gutiérrez NeurIPS'24 arXiv:2405.14831): `EntityExtractor`+`GraphRetriever` ports, `RegexEntityExtractor` (capitalized-run noun phrases), `HippoGraphRetriever` (entity co-occurrence graph + PURE-PYTHON Personalized PageRank power-iteration α=0.5, no networkx). Non-taut: query seeds PPR on "alice"→signal flows alice→acme→paris bridge→cross-passage chunk c2 scored, disconnected c3=0.0. (2) **MRL+int8 quantized store** (Matryoshka NeurIPS'22 arXiv:2205.13147): `QuantizedInMemoryVectorStore` (int8 candidate dot-product → float32 cosine rescore of top_k*mult; sparse/hybrid delegate to InMemory) + `TruncatedDimEmbedding` (Matryoshka dim slice + renorm). Non-taut: engineered int8 rank-swap that float32 rescore corrects (two-stage==pure-float32, int8-alone≠). PURE-PYTHON no numpy. (3) **Listwise reranker** (RankLLM SIGIR'25 arXiv:2505.19284): `ListwiseReranker` (whole-list IDF-weighted query coverage — a candidate's score depends on the whole list) + `ClaudeListwiseReranker` (RankGPT permutation, disabled-by-default, SDK-boundary-tested). Non-taut: rare-term carrier promoted rank3→1; removing it changes others' scores. (4) **Late chunking** (Jina arXiv:2409.04701 v3'25): `LateChunkingEmbedding(base, alpha, window)` wraps any EmbeddingProvider; new `embed_document_chunks(chunk_texts)` context-mixes each chunk with ±window neighbors (L2-renorm). ORTHOGONAL to M6 Contextual Retrieval (never touches text). Non-taut: pronoun chunk "It will ship next year" cosine to "Tesla" 0.0→>0.0 after mixing with entity-bearing neighbor. INLINE wiring: settings `reranker_kind=listwise`/`use_quantized_store`/`embedding_truncate_dims`/`use_late_chunking`+`late_chunking_alpha`; registry `_build_embedding` wraps base→truncate→late-chunk, `_build_vector_store` quantized opt-in, `_build_reranker` listwise; IngestPipeline._index_chunks DUCK-TYPES `embed_document_chunks` (called per-source = one doc's chunks, so late-chunking pools within a document); facade exports (51 symbols); HippoRAG/wrappers composable programmatically. **657 tests** (was 594, +63), all gates green offline (mypy 135 files). Pushed origin/main (31f5b5d).

**M12 shipped (research→implement, 2026-06-14):** "Context Quality & Evaluation" — uncovered 2025-26 areas. RESEARCH: 3 background executors web-researched + WROTE findings to `.omc/research/m12-{eval,context,frontier}.md` (committed; file-output approach cleaner than M10's transcript-extraction). Synthesized → 4 picks. Deferred as model/GPU-bound: RL search agents (Search-R1), visual/ColPali RAG, Qwen3 embeddings, attention-internal defenses (SDAG/FACTUM). IMPLEMENT: 4 parallel Sonnet executors (file-disjoint) + inline wiring. (A) **Context-engineering pipeline**: `ContextProcessor` port + `RedundancyCurator`(token-Jaccard dedup ≥0.8), `ExtractiveCompressor`(per-chunk sentence selection by query overlap > mean, model_copy text — preserves chunk_id so citations resolve), `LostInTheMiddleReorderer`(rank1→pos0, rank2→last, alternating edges; Liu'23/Attention-Basin Aug'25). Wired into `QueryPipeline.answer_with_contexts` BETWEEN rerank and generate via `context_processors: Sequence[ContextProcessor]`; registry builds [curate,compress,reorder] when `use_context_processing`. (B) **Claim-level RAGChecker eval** (Ru arXiv:2408.08067): pure fns in `evaluation_contracts/ragchecker_metrics.py` — `claim_precision`/`claim_recall`(FActScore), `noise_sensitivity`(claim ungrounded-in-gold but traces to a noisy chunk), `citation_precision`/`citation_recall`(per-sentence Full/Partial/None support, TREC'25), `abstention_score`(hedge phrases incl Korean, RGB). Reuses existing `_sentences`/`_tokens`/`_containment`. (C) **Proposition chunking** (Dense X Retrieval, Chen EMNLP'24): `PropositionChunker`(strategy="proposition") — one Chunk per sentence (atomic), self-containment via heading breadcrumb in `Chunk.context` (reuses Contextual-Retrieval embedding_text), full provenance. Non-taut: fact1 chunk excludes fact2 vs heading-aware merging both. Registry chunk_strategy wired. (D) **Iterative multi-turn retrieval** (Search-R1 arXiv:2503.09516; A-RAG arXiv:2602.03442; DISTINCT from CRAG single-retry & decomposition parallel — SEQUENTIAL hops w/ stop condition): `FollowupController` port + `BridgeFollowupController`(extract bridge terms from NEW chunks only, stop on coverage/max_hops) + `IterativeRetrievalPipeline`(retrieve→accumulate non-zero-score→followup loop→generate once). Non-taut 2-hop: alphacorp→zetacorp bridge, single retrieve misses hop2 (score 0), iterative gets it via bridge term in round 1. Facade exports (60 symbols). **751 tests** (was 657, +94), all gates green offline (mypy 145 files). Research files in `.omc/research/`. Pushed origin/main (9e264be).

**M13 shipped (consolidation, 2026-06-14):** the empirical comparison the whole session was missing — `evaluation/ablation.py` `AblationRunner`. Compares arbitrary NAMED `Settings` configs (baseline/contextual/proposition/multi_query/listwise/ctx_processing/…) on one corpus+question set: per config it `build_system` → ingests → **rebuilds gold per config via build_gold_dataset substring labeling** (chunk ids differ across chunk strategies — this is the key correctness point) → scores retrieval (EvalRunner recall/mrr/ndcg), RAGAS-lite generation (GenerationEvalRunner faithfulness/context_precision), and claim-level (claim_precision/noise_sensitivity/abstention). `AblationReport.comparison_table()` + `best_by(metric)`. `AblationConfig(name, Settings)` + `AblationCase(CaseSpec, reference_answer)`. Done INLINE (cohesive, deep-surface knowledge). Non-taut test (`test_ablation.py`): on the M6 entity-less-chunk corpus, the contextual config's mean_mrr + context_precision are STRICTLY greater than baseline (harness surfaces a real measured lift, best_by("mean_mrr")=="contextual"). Runnable `scripts/ablation.py`. Honest finding: on easy/saturated corpora configs tie at 1.0 → fancy techniques don't always help, which the harness shows rather than hides. Corpus typed text-only (build_gold_dataset takes str). Exported via jera.evaluation. **754 tests** (was 751, +3), all gates green offline (mypy 146 files).

**Next — M5b (opt-in zoo, follow-up increments):** opendataloader/camelot/pyhwp parser adapters; tesseract/rapidocr `OCREngine` impls (plug into RoutingPdfParser); `ClovaOCR` disabled-by-default; local VLM router (Qwen2-VL/GOT) behind `vlm` extra; real `scripts/parser_bench.py`. Each opt-in (lazy import + requires_extra/skipif, docling precedent). Then Surya/GOT/PaddleOCR/marker/MinerU as criterion-gated. See [[jera-build-conventions]].
